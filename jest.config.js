/** @type {import('ts-jest/dist/types').InitialOptionsTsJest} */
const config = {
	rootDir: './',
    displayName: 'Core',
    testMatch: ['**/__tests__/*.test.js'],
    resetModules: true,
};

export default config;